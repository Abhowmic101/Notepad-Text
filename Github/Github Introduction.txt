SCM-source code management
git init-initialize
A-added all the files need same numbers of dot as files
git add . to add something
git push to push the code
git checkout-to switch the branch.
git branch [name]-to open or name new branch
git branch -d [name]-To delete a branch 
git push--set upstream origin branch name-To push code after adding new
branch.

git merge-To merge a branch in a active branch.
git checkout -b [branch name] -take you to the branch after
creating a new branch.
git status tells you about the history of what you modified.

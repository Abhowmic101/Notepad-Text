Arithmetic operator:
+,-,*,%,**(Exponent),++(increment),--(decrement)

a--(post increment)
--a(pre increment)
Assignment Operator:
=, +=, -=, *=, /=, %=, **=  

use clear() function to clear all.

num1=parseInt(num1, 10); 
parseFloat
when adding a number and a string js will treat the number 
as string.
s
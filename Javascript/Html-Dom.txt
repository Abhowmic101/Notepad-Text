We can access an element by using getElementById(),getElementsByClassName(),getElementsByTagName().
.innerHtml is used to change the text.
We can also access/get/find elements by using getElementsByTagName
Why do we need Html-Dom
to change the content of HTML elements
How to change the style (CSS) of HTML elements
How to react to HTML DOM events
How to add and delete HTML elements
 The HTML DOM is a standard for how to get, change, add, or delete HTML elements.
In the DOM, all HTML elements are defined as objects.

Properties of Html-Dom Elements:
element.innerHtml=new html content
element.attribute=new value
element.style.property=new style
Method
element.setAttribute(attribute,value)


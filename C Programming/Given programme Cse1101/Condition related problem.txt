1.Answer:
#include <stdio.h>

int main()
{
    float a;/*condition related program 1*/
    scanf("%f",&a);
    if(a>=0){
        printf("Positive");
    }
    else if(a<0){
        printf("Negative");
    }
    else{
        printf("Invalid Input");
    }
return0
}

2.Ans:
#include <stdio.h>

int main()
{
    int a;/*condition related program 1*/
    scanf("%d",&a);
    if(a/2==0){
        printf("Even");
    }
    else if(a/2!=0){
        printf("Odd");
    }
    else{
        printf("Invalid Input");
    }
    
    
    

    return 0;
}


3.Ans:
#include <stdio.h>

int main()
{
    int a;/*condition related program 1*/
    scanf("%d",&a);
    if(a==0){
        printf("Zero");
    }
    else if(a==1){
        printf("One");
    }
    else if(a==2){
        printf("Two");
    }
    else if(a==3){
        printf("Three");
    }
    else if(a==4){
        printf("Four");
    }
    else if(a==5){
        printf("Five");
    }
    else if(a==6){
        printf("Six");
    }
    else if(a==7){
        printf("Seven");
    }
    else if(a==8){
        printf("Eight");
    }
    else if(a==9){
        printf("Nine");
    }
    else{
        printf("Invalid Input");
    }
    
    return 0;
}

4.Ans:

 #include <stdio.h>

int main()
{
    int a,b,c,sum=0,flag1,flag2,flag3;
    
    scanf("%d%d%d",&a,&b,&c);
    if(a>0 && a<180){
        flag1=1;
    }
      else{
        flag1=0;
    }
    
    if( b>0 && b<180){
        flag2=1;
        }
      else{
        flag2=0;
    } 

    if(c>0 && c<180){
        flag3=1;
    }
      else{
        flag3=0;
    }
   
   
    if(flag1==1 && flag2==1 && flag3==1){
        sum=a+b+c;
    }
    else{
        printf("Invalid Input");
    }
    
    if(sum==180){
    
        printf("Triangle is valid");
    } 
    
    else{
        printf("Triangle is not valid");
    }
    
    return 0;
    
}

5.
#include <stdio.h>

int main()
{
    int num,d;
    scanf("%d",&num);
    if(num==1|| num==2){
        printf("Yes");
    }
    else if(num==0){
        printf("No");
    }
    else if(num%2==0){
        d=num/2;
        if(d%2==0){
            printf("Yes");
        }
        else{
            printf("No ");
        }
    }
  else{
       printf("No")   
          }
        }
 else{
        printf("Invalid Input");
    }

    return 0;
}

6.

#include <stdio.h>

int main()
{
    int num,d;
    scanf("%d",&num);
    if(num>0){
    if(num==1|| num==2){
        printf("%d is power of 2",num);
    }
   
    else if(num%2==0){
        d=num/2;
        if(d%2==0){
            printf("%d is  power of 2",num);
        }
        else{
            printf("%d is not power of 2 ",num);
        }
    }
    }
    else if(num==0){
        printf("Zero is not a valid input");
    }
    else if(num<0){
        printf("Negative input is not valid");
    }
    else{
        printf("Invalid Input");
    }

    return 0;
}


7. #include <stdio.h>

int main()
{
   int x,y;
   scanf("%d %d",&x,&y);
   if (x<y){
       printf("%d is less than %d",x,y);
   }
   else if(x>y){
        printf("%d is greater than %d",x,y);
   }
   
   else if(x==y){
        printf("%d is equal to %d",x,y);
   }
   else {
       printf("Invalid Input");
   }
    return 0;
    
}

8.

#include <stdio.h>

int main()
{
  int year;
  scanf("%d",&year);
  if( year % 4 == 0 && year % 100 != 0 || ( year % 400 ==0)) {
      printf("Yes");
  }
  
  else{
     printf("No");
  }
    return 0;
}

9.
#include <stdio.h>

int main()
{
  char ch;
  scanf("%c",&ch);
  if( (ch>='a' && ch<='z' ) || (ch>='A' && ch<='Z')){
      printf("%c is a character",ch);
  }
  else if(ch>='0' && ch<='9') {
      printf("%c is a digit",ch);
      
  }
  else{
     printf("%c is a special character",ch);
  }
    return 0;
}

10.

#include <stdio.h>

int main()
{
    int n1,n2;
    char ch;
    scanf("%d %c %d",&n1,&ch,&n2);
    if(ch=='+'){
       
        printf("Sum:%d",n1+n2);
    }
    
    else if(ch=='-'){
       
        printf("Subtraction:%d",n1-n2);
    }
    
    else if(ch=='*'){
       
        printf("Multiplication:%d",n1*n2);
    }
    else if(ch=='/'){
        if(n2==0){
            printf("Zero is not valid as divisor");
        }
        
        else{
            
        printf("Division:%d",n1/n2);
    }
 }

else{
      printf("Invalid Operator");
       }
   
  
    return 0;
}



12.#inclde<stdio.h>
int main()
{
   int a,b,choice,sum,sub,m,d;
   printf("Enter your numbers:");
   scanf("%d%d",&a,&b);
   printf("Enter your choice:");
   scanf("%d",&choice);
   
   if(choice==1){
       sum=a+b;
       printf("Sum:%d",sum);
   }
     
  else if(choice==2){
       sub=a-b;
       printf("Subtraction:%d",sub);
   }
     
   else if(choice==3){
       m=a*b;
       printf("Multiplication:%d",m);
   }
     
   else if(choice==4){
       d=a/b;
       printf("Quotient:%d",d);
   }
   else{
       printf("Invalid Input");
   }
   

    return 0;
}

11.
#include <stdio.h>

int main ()
{
  float a;
  printf ("Enter your numbers:");
  scanf ("%f", &a);

  if (a >= 0 && a < 55)
    {

      printf ("Grade:F");
    }

  else if (a >= 55 && a <= 57)
    {

      printf ("Grade:D");
    }
  else if (a >= 62 && a <= 65)
    {
    printf ("Grade:C-");
        
    }
  else if (a >= 62 && a <= 65)
    {
    printf ("Grade:C");
        
    }
    else if (a >= 66 && a <= 69)
    {
    printf ("Grade:C+");
        
    }
    
    else if (a >= 70 && a <= 73)
    {
    printf ("Grade:B-");
        
    }
    
    else if (a>= 74 && a<= 77)
    {
    printf ("Grade:B");

    }
    
    else if (a>=78 && a<=81)
    {
    printf ("Grade:B+");
        
    }
    
    else if (a>=82 && a<=85)
    {
    printf ("Grade:A-");
        
    }

    else if (a>=86 && a<=89)
    {
    printf ("Grade:A");
        
    }
    else if (a>=90 && a<=100)
    {
    printf ("Grade:A+");
        
    }
  else
    {
      printf ("Invalid Input");
    }


  return 0;
}


13.
#include <stdio.h>

int main()
{
  int a,b,choice ;
  scanf("%d %d",&a, &b);
  scanf("%d ",&choice);
 
      if(choice==1){
       printf("Sum:%d",a+b);   
      }
      
      
     else if(choice==2){
       printf("Subtraction:%d",a-b);   
      }
      
      else if(choice==3){
       printf("Multiplication:%d",a*b);   
      }
      
      else if(choice==4){
        
         scanf("%d",&case1);
          if(case1==1){
             printf("Quotient:%d",a/b);
          }
          else if(case2==2){
       printf("Remainder:%d",a%b);
          }
          else{
              printf("Invalid Input");
          }  
      }
      else{
          printf("Invalid Input");
      }
  
    return 0;
}

14.

#include <stdio.h>

int main()
{
  int a,b,choice ;
  scanf("%d %d",&a, &b);
  scanf("%d ",&choice);
 
      if(choice==1){
       printf("Sum:%d",a+b);   
      }
      
      
     else if(choice==2){
       printf("Subtraction:%d",a-b);   
      }
      
      else if(choice==3){
       printf("Multiplication:%d",a*b);   
      }
      
      else if(choice==4){
          if(b!=0){
         scanf("%d",&case1);
          if(case1==1){
             printf("Quotient:%d",a/b);
          }
          else if(case2==2){
       printf("Remainder:%d",a%b);
          }
          else{
              printf("Invalid Input");
          }
          }
          else{
              printf("zero is not valid as a divisor");
          }
      }
      else{
          printf("Invalid Input");
      }
  
    return 0;
}

15.

#include <stdio.h>

int main()
{
  int x,n1,n2,n3  ;
  scanf("%d ",&x);
  scanf("%d ",&n1);
  
  if(n1==x){
      printf("Right, Player-2 wins!");
  }
  else{
      printf("Wrong, 2 Chance(s) Left!");
      scanf("%d",&n2);
      if(n2==x){
          printf("Right, Player-2 wins!");
      }
  
      else{
          printf("Wrong, 1 Chance(s) Left!");
          scanf("%d",&n3);
         if(n3==x){
             printf("Right, Player-2 wins!");
            
         }
         else{
             printf("Wrong, 0 Chance(s) Left!");
             printf(" Player-1 wins!");
         }
      }
  }
 
  
    return 0;
}

1.


#include <stdio.h>

int main()
{
    float x,y;
    scanf("%f %f",&x,&y);
    printf("Sum:%f\nSub:%f\nMultiplication:%f\nQuotient:%f",x+y,x-y,x*y,x/y,x%y);

    return 0;
}


2.
#include <stdio.h>

int main()
{
   float r,a,pi=3.1416;
   scanf("%f",&r);
    a= 2 * pi * r;
   printf("Area:%f",a);
   return 0;
}

3.

#include <stdio.h>

int main()
{
    float a,b,x;
    scanf("%f %f",&a,&b);
 x= (3.31 * (a*a) + 2.01 * (b*b*b)) / (7.16 * (b*b) + 2.01 * (a*a*a));
 printf("X=%f");
   return 0;
}

4.
Decrement:%d\nDecrement:%d

,++x,x--,--x


8